$('.articles').append('<%= j render partial: "shared/article", collection: @articles %>');
<% if @articles.next_page %>
  $('.pagination').replaceWith('<%= j paginate @articles, :theme => 'twitter-bootstrap-3' %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
